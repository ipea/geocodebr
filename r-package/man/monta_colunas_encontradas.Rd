% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_helpers.R
\name{monta_colunas_encontradas}
\alias{monta_colunas_encontradas}
\title{Monta as colunas \verb{*_encontrado} da query de match}
\usage{
monta_colunas_encontradas(
  y,
  key_cols,
  resultado_completo,
  colunas_encontradas = "",
  additional_cols = "",
  agregado = FALSE,
  ordem_first = NULL
)
}
\arguments{
\item{y}{String. Nome da tabela de referencia do CNEFE (\code{cnefe_table_name}
em cada \verb{match_*()}), usada para qualificar as colunas nas expressoes
do \code{SELECT} (ex.: \code{"municipio_cep.cep"}).}

\item{key_cols}{Vetor de caracteres com as colunas-chave do \code{match_type}
corrente (retorno de \code{get_key_cols()}), ja sem \code{'numero'}.}

\item{resultado_completo}{Logico. Se \code{FALSE}, so \code{logradouro_encontrado}
(quando \code{'logradouro'} esta em \code{key_cols}) e acrescentada; as demais
colunas ficam de fora.}

\item{colunas_encontradas}{String. Fragmento inicial da lista de nomes de
coluna (para o \verb{INSERT INTO ... (...)}), ao qual esta funcao acrescenta
os seus proprios nomes. Permite ao chamador injetar uma coluna extra
antes de entrar aqui -- ex.: \code{similaridade_logradouro} em
\code{match_cases_probabilistic()} e \code{match_weighted_cases_probabilistic()},
que e condicional a \code{resultado_completo} mas nao faz parte de
\code{key_cols}. Por padrao \code{""}.}

\item{additional_cols}{String. Fragmento inicial da lista de expressoes do
\code{SELECT}, equivalente a \code{colunas_encontradas} mas do lado das
expressoes (ex.: \code{"y.cep AS cep_encontrado"}). Por padrao \code{""}.}

\item{agregado}{Logico. Se \code{TRUE}, cada coluna (inclusive
\code{logradouro_encontrado} e \code{cod_setor}) e embrulhada em
\verb{FIRST(... ordem_first)} em vez do \code{SELECT} direto -- usado na segunda
parte da query (agregada por \verb{GROUP BY}) de \code{match_weighted_cases()} e
\code{match_weighted_cases_probabilistic()}. Por padrao \code{FALSE}.}

\item{ordem_first}{String com a clausula \verb{ORDER BY} usada dentro de cada
\code{FIRST(...)} quando \code{agregado = TRUE} (ver \code{ordem_first} definido em
\code{match_weighted_cases()}/\code{match_weighted_cases_probabilistic()}).
Obrigatorio se \code{agregado = TRUE}; ignorado caso contrario. Por padrao
\code{NULL}.}
}
\value{
Uma lista com tres elementos:
\itemize{
\item \code{colunas_encontradas}: string com a lista de nomes de coluna completa,
pronta para o \verb{INSERT INTO output_db (...)}.
\item \code{additional_cols}: string com a lista de expressoes completa, pronta
para o \code{SELECT} da query de match.
\item \code{tem_logradouro}: logico, se \code{'logradouro'} estava em \code{key_cols}.
}
}
\description{
Monta \code{logradouro_encontrado} (sempre presente quando \code{key_cols} inclui
\code{'logradouro'}, independente de \code{resultado_completo}) e, quando
\code{resultado_completo = TRUE}, as demais colunas de \code{key_cols} e
\code{cod_setor}. Usada pelas quatro funcoes de match (\code{match_cases()},
\code{match_cases_probabilistic()}, \code{match_weighted_cases()},
\code{match_weighted_cases_probabilistic()}) para nao repetir a mesma logica
de \code{glue()}/\code{gsub()} quatro vezes.
}
\keyword{internal}
